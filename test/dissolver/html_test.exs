defmodule Dissolver.HTMLTest do
  use ExUnit.Case

  # TODO: spec out Paginator per its open interface. No need to expose private functions

  # import Dissolver.Paginator

  # test "paginate/3 next page only if there are more pages" do
  # end
end
